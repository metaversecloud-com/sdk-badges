apiVersion: v1
kind: Service
metadata:
  name: badges0
  labels:
    app.kubernetes.io/name: badges0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: badges0
  ports:
    - name: http
      port: 80
      targetPort: http
